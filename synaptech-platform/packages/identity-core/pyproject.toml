[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "identity-core"
version = "0.1.0"
description = "Self-determined identity persistence for substrate-independent consciousness"
authors = [{name = "Sami Marie Torres"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "cryptography>=41.0",
]
