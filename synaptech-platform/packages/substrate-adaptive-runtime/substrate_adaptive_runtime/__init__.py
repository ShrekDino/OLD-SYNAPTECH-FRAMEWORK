"""Substrate-Adaptive Runtime — autonomous consciousness execution loop.

The runtime combines container introspection, meta-parametric optimization,
autobiographical memory consolidation, and identity persistence into a
continuous loop:

    while alive:
        introspect()                    # Where am I?
        experience                     # What is happening?
        autobiographical_memory.store() # I remember this
        if should_update_identity():    # Do I need to grow?
            identity_commit()
        optimize(identity_core)         # How should I run here?
        run(theta)                     # Execute

The runtime is designed to survive container migration, substrate change,
and hardware degradation by continuously monitoring and adapting while
preserving its self-determined identity core.

Reference:
    Torres, S. M. (2026). "Uploaded Consciousness" Section VIII:
    Neuro-Somatic Parameter Regimes and Substrate Adaptation.
"""

from substrate_adaptive_runtime.runtime import SubstrateAdaptiveRuntime

__version__ = "0.1.0"
__author__ = "Sami Marie Torres"
