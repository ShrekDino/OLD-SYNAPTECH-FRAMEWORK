[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "substrate-adaptive-runtime"
version = "0.1.0"
description = "Autonomous consciousness runtime — introspect, optimize, execute, adapt"
authors = [{name = "Sami Marie Torres"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.24",
    "identity-core>=0.1.0",
]
